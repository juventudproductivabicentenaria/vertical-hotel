<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_rule_hotel_housekeeping_activities" model="ir.rule">
            <field name="name">Record Name</field>
            <field name="model_id" ref="model_hotel_housekeeping_activities"/>
            <field name="domain_force">['|',('user_id','=',user.id),('inspector_id','=',user.id)]</field>
            <field name="groups" eval="[(4, ref('hotel.group_hotel_user'))]"/>
        </record>
    </data>
</odoo>